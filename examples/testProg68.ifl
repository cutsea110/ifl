num2chr n = unfoldr psi n;
psi n = if (n == 0) Nothing (Just (swap (divMod n 10)));
showNum n = foldr (flip seq) 0 (map (compose putChar (plus 48)) (num2chr n));

main = seq (showNum 257) (putChar 10)
